# Legacy JSON 适配（P1 阶段）

M0 **不实现**。本目录占位，供 P1 添加 JSON 只读适配：

```text
legacy/
├── production-json-adapter.ts   # ~/Desktop/AI-Veo/Projects/*.json
└── workbench-local-adapter.ts   # localStorage workbench:t2v（只读）
```

迁移前 JSON 继续为 SoT；适配器 **只读**，不写 DB。
